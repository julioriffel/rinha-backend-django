################## Auto generated ##################
[databases]
postgres = host=db port=5432 user=postgres

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 5432
unix_socket_dir =
user = postgres
auth_file = /etc/pgbouncer/userlist.txt
auth_type = md5
pool_mode = transaction
min_pool_size = 50
#default_pool_size = 100
#max_db_connections = 100
#max_client_conn = 1000
max_client_conn = 1100
# Log settings
admin_users = postgres,myuser

# Connection sanity checks, timeouts
log_connections = 0
log_disconnections = 0

# TLS settings

# Dangerous timeouts
################## end file ##################